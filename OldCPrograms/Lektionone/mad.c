#include <stdio.h>
#include <stdlib.h>
#include <time.h>

enum forret{guacamole, tartelette, salmonroll, pumpkinsoup};
enum hovedret{cabbage, chopped_steak, blossom, meatloaf};
enum dessert{pancakes_with_icecream, carrotcake, chocolatemousse, lemonmousse};

void maaltid(int *randforret, int randhovedret, int randdessert);

int main(void)
{
	int n;
	int randforret;
	int randhovedret;
	int randdessert;

	for(n = 1; n < 25; n++)

}

void maaltid(int )
{
	*randforret = rand() % 
}